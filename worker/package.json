{
  "name": "wain-diagnosis-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Growth Diagnosis endpoint — holds the Anthropic and Resend keys the static site cannot.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.120.1"
  }
}
