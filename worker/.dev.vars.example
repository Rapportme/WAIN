# Copy to `.dev.vars` for local `wrangler dev`. Never commit the real file —
# .dev.vars is gitignored; this example is not.
#
# In production this is a Worker secret, set with:
#   npx wrangler secret put RESEND_API_KEY

RESEND_API_KEY=re_...
